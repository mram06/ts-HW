// 🏡 3. Будівельник для об’єкта "Будинок" (HouseBuilder)
// Опиши процес будівництва будинку з етапами: фундамент, стіни, дах, вікна, двері.
// Реалізуй HouseBuilder, який дозволяє послідовно задавати ці частини, а потім методом build()
// отримати готовий об’єкт House.

import HouseBuilder from "./4_Builder/HouseBuilder.js";

const house = new HouseBuilder()
  .setDoors("wood")
  .setWalls("brick")
  .setWindows("eu")
  .build();

console.log(house);
